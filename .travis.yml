# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

os: osx
osx_image: xcode9.4
cache: cocoapods
podfile: Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install
script:
- set -o pipefail && xcodebuild test -workspace EndpointProcedure.xcworkspace -scheme ALL -sdk iphonesimulator11.4 ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
